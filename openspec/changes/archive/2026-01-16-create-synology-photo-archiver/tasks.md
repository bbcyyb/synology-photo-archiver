## 1. 项目初始化
- [x] 1.1. 创建项目目录 `synology-photo-archiver`。
- [x] 1.2. 在目录中创建主文件 `archiver.py` 和 `config.ini.template` 模板文件。
- [x] 1.3. 创建 `tests/` 目录用于存放测试代码。

## 2. 核心功能开发
- [x] 2.1. 实现配置模块，用于安全地读取 `config.ini`。
- [x] 2.2. 实现状态管理模块，负责读取和写入 `processed_files.json`。
- [x] 2.3. 实现文件扫描模块，根据状态文件识别新增和修改过的照片。
- [x] 2.4. 实现 7z 调用模块，使用 `subprocess` 构建并执行打包和加密命令。
- [x] 2.5. 编写主逻辑，将以上所有模块串联起来，完成整个归档流程。

## 3. 测试与验证
- [x] 3.1. 为文件扫描和状态管理模块编写单元测试。
- [x] 3.2. 编写集成测试，模拟完整的文件系统操作和 7z 调用。
- [x] 3.3. 手动在测试环境中运行脚本，验证其功能符合预期。

## 4. 文档
- [x] 4.1. 编写 `README.md`，包含安装、配置和使用说明。必须使用中文书写。
- [x] 4.2. 提供在群晖“任务计划程序”中设置定时任务的详细步骤。
